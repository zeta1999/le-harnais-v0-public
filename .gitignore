# generated by dist/publish.sh — reassembled (join.sh) binaries are local artifacts
/tools/appsec/appsec-linux-x86_64
/tools/appsec/appsec-macos-arm64

# weights fetched by models/fetch_models.sh, which downloads to models/<name>/
# (antares-1b alone is 1.9G). The tooling beside them stays tracked - see the negation.
/models/*/
!/models/modelfiles/
